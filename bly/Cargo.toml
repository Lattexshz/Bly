[package]
name = "bly"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
raw-window-handle = "0.5.0"

log = "0.4.17"
env_logger = "0.10.0"

bly-ac = { path = "../backends/bly-ac" }

[target."cfg(windows)".dependencies]
bly-dx2d = { path = "../backends/bly-dx2d" }

[target."cfg(unix)".dependencies]
bly-cairo = { path = "../backends/bly-cairo" }

[dev-dependencies]
winit = "0.28.1"