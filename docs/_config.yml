domain: lattexshz.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https:/lattexshz.github.io  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: /Bly/

markdown: kramdown
theme: minima